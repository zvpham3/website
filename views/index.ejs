<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sales Report (Dynamic)</title>
    <style>
        /* ... all your existing styles ... */
        .anchor-pane {
            width: 600px;
            height: 400px;
            border: 1px solid #ccc;
            position: relative; 
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 10px;
        }
        .back-button { position: absolute; top: 10px; left: 10px; }
        .sales-table-container { position: absolute; top: 50px; left: 26px; }
        .date-input-group { position: absolute; bottom: 50px; left: 0; right: 0; padding: 0 20px; display: flex; align-items: center; justify-content: flex-start; }
        .sales-report-table { width: 557px; border-collapse: collapse; }
        .sales-report-table th, .sales-report-table td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        .sales-report-table th { background-color: #f2f2f2; }
        .date-label { margin-right: 5px; }
        .date-field { margin-right: 20px; padding: 5px; }
        .generate-button { margin-left: auto; }
        /* Utility classes for positioning (mimicking layoutX/layoutY) */
        .back-button { position: absolute; top: 10px; left: 10px; }
        
        /* MODIFIED: Fixed height and scroll added to the container */
        .sales-table-container { 
            position: absolute; 
            top: 50px; 
            left: 26px; 
            
            /* New properties for fixed size and scrolling */
            max-height: 200px; /* Set the maximum height for the scrollable area */
            width: 557px;      /* Match the table width to prevent horizontal scroll issues */
            overflow-y: auto;  /* Add vertical scrollbar when content exceeds max-height */
            padding-right: 15px; /* Add padding to account for the scrollbar width */
        }
        
        /* Table styling */
        .sales-report-table { 
            width: 100%; /* Change to 100% to fill the container width */
            border-collapse: collapse; 
            /* Ensure table header sticks to the top during scroll (Optional: Requires more complex CSS) */
        }
    </style>
</head> 
<body>

    <div class="anchor-pane">
        <button class="back-button">Back</button>
    
        <div class="sales-table-container">
            <h3>Sales Report</h3>
            
            <table class="sales-report-table">
                <thead>
                    <tr>
                        <th style="width: 50%;">Menu Item</th>
                        <th style="width: 50%;">Number of Item Sold</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (typeof reportItems !== 'undefined' && reportItems.length > 0) { %>
                        <% reportItems.forEach(item => { %>
                            <tr>
                                <td><%= item.menu_item %></td>
                                <td><%= item.quantitySold %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="2" style="text-align: center;">No sales data for this period.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        
        <div class="total-cost">
            Total Sales: $<%= typeof totalCost !== 'undefined' ? totalCost.toFixed(2) : '0.00' %>
        </div>
        
        <div class="date-input-group">
            </div>
    </div>
    
    </body>
</html>